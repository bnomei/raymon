# Archived crates (not maintained)

This directory contains an older workspace-style split of Raymon.

It is **archived** and **not maintained**.

Source of truth: the root crate (`Cargo.toml` + `src/*`).

