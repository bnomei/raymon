# raymon-core Tasks
Note: Check tasks only after `cargo test -p <crate>` (or the specified crate) is green.

- [x] Scaffold `crates/raymon-core` with `Cargo.toml` and `src/lib.rs`.
- [x] Implement core domain types (envelope, payload, origin, entry).
- [x] Implement filter types and default screen naming helper.
- [x] Define `StateStore` and `EventBus` traits.
- [x] Add unit tests for default screen naming and filter behavior.
- [x] Run `cargo test -p raymon-core` and mark tasks complete when green.
