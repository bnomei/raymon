# raymon-cli Tasks
Note: Check tasks only after `cargo test -p <crate>` (or the specified crate) is green.

- [x] Scaffold `crates/raymon-cli` with `Cargo.toml` and `src/main.rs`.
- [x] Implement config resolution and CLI flags.
- [x] Wire shared state, storage, ingest, and MCP routes into one server.
- [x] Start TUI conditionally and handle shutdown.
- [x] Add tests for config resolution and flag parsing.
- [x] Run `cargo test -p raymon-cli` and mark tasks complete when green.
