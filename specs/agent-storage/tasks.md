# raymon-storage Tasks
Note: Check tasks only after `cargo test -p <crate>` (or the specified crate) is green.

- [x] Scaffold `crates/raymon-storage` with `Cargo.toml` and `src/lib.rs`.
- [x] Implement JSONL append + read helpers.
- [x] Implement offset index rebuild and lookup.
- [x] Implement blob store API for non-text payloads.
- [x] Add tests for append/read and index rebuild.
- [x] Run `cargo test -p raymon-storage` and mark tasks complete when green.
