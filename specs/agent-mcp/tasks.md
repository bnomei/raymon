# raymon-mcp Tasks
Note: Check tasks only after `cargo test -p <crate>` (or the specified crate) is green.

- [x] Scaffold `crates/raymon-mcp` with `Cargo.toml` and `src/lib.rs`.
- [x] Implement MCP request parsing and routing.
- [x] Implement tool handlers for list/get/screens/emit.
- [x] Implement event stream endpoint (SSE) if in scope.
- [x] Add unit tests for request validation and tool handlers.
- [x] Run `cargo test -p raymon-mcp` and mark tasks complete when green.
